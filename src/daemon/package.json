{
  "name": "@bicycle/daemon",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf node_modules .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@bicycle/shared": "workspace:*",
    "@spader/zargs": "catalog:",
    "age-encryption": "catalog:",
    "hono": "catalog:",
    "liquidjs": "^10.27.0",
    "pino": "catalog:",
    "pino-pretty": "catalog:",
    "yargs": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
