post_install() {
  systemctl enable --now bicycle.service
}

post_upgrade() {
  systemctl daemon-reload
  systemctl try-restart bicycle.service
}

pre_remove() {
  systemctl disable --now bicycle.service 2>/dev/null || true
}
