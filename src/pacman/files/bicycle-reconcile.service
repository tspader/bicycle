[Unit]
Description=Bicycle: reconcile all app stacks to desired state
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/bicycle reconcile

[Install]
WantedBy=multi-user.target
